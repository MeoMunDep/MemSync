version: '3.8'

services:
  MemSync:
    build:
      context: .
    container_name: MemSync
    restart: unless-stopped
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production
    stdin_open: true
    tty: true
